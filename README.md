### Credentials:

To create an AWS access key:
IAM → Users → select your user → Security credentials → Access keys → Create access key → choose Local code.
```
export AWS_ACCESS_KEY_ID="<anaccesskey>"
export AWS_SECRET_ACCESS_KEY="<asecretkey>"
```
